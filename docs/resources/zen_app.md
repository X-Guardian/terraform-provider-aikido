---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "aikido_zen_app Resource - aikido"
subcategory: "Zen Firewall"
description: |-
  Manages a Zen runtime application firewall (WAF) app in Aikido Security.
---

# aikido_zen_app (Resource)

Manages a Zen runtime application firewall (WAF) app in Aikido Security.

## Example Usage

```terraform
resource "aikido_zen_app" "api" {
  name        = "API Server"
  environment = "production"
  repo_id     = "410013"
}

output "zen_token" {
  value     = aikido_zen_app.api.token
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `environment` (String) The environment: `production`, `staging`, or `development`.
- `name` (String) The name of the Zen app.

### Optional

- `repo_id` (String) The ID of a code repository to link to this app.

### Read-Only

- `blocking` (Boolean) Whether blocking mode is enabled.
- `code_repo_name` (String) The name of the linked code repository.
- `has_token` (Boolean) Whether a token is set for this app.
- `id` (String) The unique identifier of the Zen app.
- `token` (String, Sensitive) The Zen app token. Only available after initial creation.
- `token_hint` (String) A hint of the current active token.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import a Zen app by its ID (token will not be available after import)
terraform import aikido_zen_app.api "123"
```
